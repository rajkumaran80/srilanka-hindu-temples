import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'srilankan.hindu.temples',
  appName: 'Sri Lanka Hindu Temples',
  webDir: 'dist'
};

export default config;
